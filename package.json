{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.5.1"
  },
  "scripts": {
    "watch:viteapp" : "lerna watch --scope=viteapp --include-dependencies -- lerna run build --scope=\\$LERNA_PACKAGE_NAME"
  }
}
